using Microsoft.AspNetCore.Identity;

namespace GinWhiskeyExperten.Models
{
    // Empty for now - IdentityUser already provides Id, Email, UserName, PasswordHash, etc.
    // A later round (public registration) will add profile fields here.
    public class ApplicationUser : IdentityUser
    {
    }
}
